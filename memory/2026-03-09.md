
- 用户希望记住：如需撤销汉化版，可执行“一键回退”命令：`npm uninstall -g --prefix "$HOME/.local" @qingchencloud/openclaw-zh && sed -i '/Prefer user-installed OpenClaw (zh)/,+1d' ~/.bashrc`，然后重开终端生效。
- 用户新增偏好：凡是“视频下载类”请求，默认同时提供“可直接保存的下载链接”（优先本地直链，如 http://127.0.0.1:<port>/...）。
